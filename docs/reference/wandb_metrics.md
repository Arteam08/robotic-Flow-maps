# wandb metric glossary

Generated by `python -m eqfm.wandb_glossary`. Each run also carries its own filled-in copy in the wandb run notes (Overview tab) and in `<run_dir>/wandb_glossary.md`. The last column gives the key used by runs logged before the readable names, so older runs can be decoded too.

# Stage 1: equilibrium field (`scripts/train_stage1_eqm.py`)

Example values: uniform_weighted sampler, ε = 1e-3, λ = 0.5.

### Notation

| symbol | meaning |
|---|---|
| x1 | clean data latent: SD-VAE latent × 0.18215, shape 4×32×32, d = 4096 entries |
| z | noise ~ N(0, I), same shape; ‖z‖ ≈ √d = 64 |
| t | interpolation time in [0, 1): t = 0 is pure noise, t → 1 is data |
| x_t | (1 − t)·z + t·x1, the network input |
| U_t | (1 − t)·(x1 − z), the regression target; it vanishes at data, so data are fixed points |
| b(x) | the network output: an autonomous field (no time input; the t-embedder is fed 0). Sampling integrates dx/dτ = b(x) from noise |
| mse | per-sample (1/d)·‖b(x_t) − U_t‖², the squared error averaged over the d latent entries |
| w(t) | (1 − ε)/(log(1/ε)·(1 − t)), ε = 0.001: importance weight that makes E_t[w·mse] with uniform t equal the canonical objective with t ~ p(t) ∝ 1/(1 − t) |
| ‖·‖ | L2 norm over the d entries of one sample, then averaged over the batch |
| θ, θ_ema | current weights; exponential moving average of the weights (decay 0.9999), used for samples and FID |
| λ | weight of the data anchor E‖b(x1)‖²/d in the total loss (λ = 0.5) |

### Metrics

Sections are numbered so the workspace lists them in reading order. Good: ↓ lower is better, ↑ higher, → target value, blank = context only.

| wandb key | definition | good | key in older runs |
|---|---|---|---|
| `0 init/head scale s` | Output layer (final_layer.linear weight and bias) multiplied by s once before training. Converts a field trained with another clock to ours; the released EqM regresses 4·min(1, 5(1−t))·(x1 − z), hence s ≈ 1/9. 1 = no rescale. |  | `(init_probe.json only)` |
| `0 init/objective at start  E[w·mse]` | Probe objective (see 4 probe) of the starting weights, after the head rescale. | ↓ | `(init_probe.json: init/uw_loss)` |
| `0 init/RMS b(x1) at start` | RMS of the starting field on clean probe latents; 0 = data are exact fixed points. | → 0 | `(init_probe.json: init/b_data_rms)` |
| `0 init/‖x1‖ data reference` | Mean norm of clean probe latents: where a sampler should end. Compare 5 samples/‖x‖ at last step. |  |  |
| `1 loss/total (optimized)` | The scalar that is back-propagated: E[w·mse] + λ·E‖b(x1)‖²/d (λ = 0.5). | ↓ | `train/loss, train/total_loss` |
| `1 loss/objective  E[w·mse]` | Batch estimate of the Stage-1 objective E[w·mse], t ~ U[0, 1−ε] weighted by w(t), ε = 0.001. Compare runs by this (or by 4 probe/objective, which has no batch noise). | ↓ | `train/eqm_loss` |
| `1 loss/unweighted  E[mse]` | Plain batch mean of mse without the time weight; dominated by small t, where mse is largest. | ↓ | `train/eqm_raw` |
| `1 loss/worst sample  max mse` | Largest per-sample mse seen since the last log. Spikes flag outlier images or instability. | ↓ | `train/eqm_loss_max` |
| `1 loss/mse, class label` | Mean mse over samples that kept their class label: the conditional branch b(x \| y) used by CFG. | ↓ |  |
| `1 loss/mse, null label` | Mean mse over samples whose label was replaced by the null class: the unconditional branch CFG needs. Missing on steps where no label was dropped. | ↓ |  |
| `1 loss/anchor  E‖b(x1)‖²/d` | Mean squared field on clean data; pins data as fixed points. Enters the total with weight λ. | → 0 | `train/data_anchor` |
| `1 loss/time head  E(t̂ − t)²` | Auxiliary head predicting the noise level from features; enters the total with weight μ. | ↓ | `train/time_pred_loss` |
| `1 loss/time head  E\|t̂ − t\|` | Mean absolute error of the auxiliary noise-level prediction. | ↓ | `train/time_pred_mae` |
| `2 loss by t/mse  t∈[lo, hi)` | Mean mse over the window's samples with t in the bucket. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | ↓ | `train/eqm_raw_t{lo}_{hi}` |
| `2 loss by t/share of objective  t∈[lo, hi)` | Fraction of the objective Σ w·mse contributed by samples with t in the bucket (the shares sum to 1): which noise levels the gradient actually trains. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. |  |  |
| `3 field by t/‖b(x_t)‖  t∈[lo, hi) or all t` | Mean norm of the network output. Should track ‖U_t‖ in the same bucket. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | ≈ ‖U_t‖ | `train/eqm_pred_norm[_t{lo}_{hi}]` |
| `3 field by t/‖U_t‖ target  t∈[lo, hi) or all t` | Mean norm of the regression target U_t = (1 − t)(x1 − z). Fixed by the data and the time law, not by the model. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. |  | `train/eqm_target_norm[_t{lo}_{hi}]` |
| `3 field by t/cos(b, U_t)  t∈[lo, hi) or all t` | Mean cosine between output and target: direction agreement, independent of scale. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | → 1 |  |
| `3 field by t/‖b − U_t‖ / ‖U_t‖  t∈[lo, hi) or all t` | Mean relative error per sample: 0 = exact, 1 = no better than predicting 0. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | → 0 |  |
| `4 probe/objective  E[w·mse], raw \| EMA θ` | Objective on 4 fixed training batches with fixed noise and fixed t, no gradient. Free of batch noise, so a change means the weights changed. | ↓ | `val/uw_loss_raw, val/uw_loss_ema` |
| `4 probe/RMS b(x1)  raw \| EMA θ` | RMS of the field on the clean probe latents; 0 = data are exact fixed points. | → 0 | `val/b_data_rms_raw, val/b_data_rms (EMA)` |
| `4 probe/mse  t∈[lo, hi), raw \| EMA θ` | Probe mse restricted to the fixed samples with t in the bucket. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | ↓ |  |
| `4 probe/‖b − U_t‖ / ‖U_t‖  t∈[lo, hi), raw \| EMA θ` | Probe relative error restricted to the fixed samples with t in the bucket. Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | → 0 |  |
| `5 samples/grid (EMA θ)` | 16 samples: Euler on dx/dτ = b(x), K = 100 steps to ε_stop = 0.001, CFG w = 1.0, fixed noise and classes so grids are comparable across steps. |  | `val/samples` |
| `5 samples/‖b‖ at first step` | Mean field norm at the start of the sampler (pure noise). |  | `val/velocity_norm_first` |
| `5 samples/‖b‖ at last step` | Mean field norm where the sampler stops; → 0 means the trajectories ended at fixed points of b. | → 0 | `val/velocity_norm_last` |
| `5 samples/max ‖b‖ along path` | Largest mean field norm over the sampler steps. |  | `val/velocity_norm_max` |
| `5 samples/‖x‖ at last step` | Mean norm of the final latents. Should match 0 init/‖x1‖ data reference. | ≈ ‖x1‖ | `val/latent_norm_last` |
| `5 samples/CFG scale w` | Guidance used for the grid: b_null + w·(b_class − b_null). |  | `val/cfg_scale` |
| `6 optim/learning rate` | AdamW learning rate (linear warmup over 500 steps, then constant). |  | `train/lr` |
| `6 optim/‖grad‖ before clip` | Global L2 norm of the gradient over trainable parameters before clipping at 1.0, mean over the optimizer steps since the last log. |  | `train/grad_norm` |
| `6 optim/max ‖grad‖ before clip` | Largest pre-clip gradient norm since the last log. |  |  |
| `6 optim/fraction of steps clipped` | Fraction of optimizer steps since the last log whose gradient norm exceeded 1.0. |  |  |
| `6 optim/‖θ‖` | Global L2 norm of the trainable parameters. |  |  |
| `6 optim/‖θ − θ_ema‖ / ‖θ‖` | Relative distance between current and EMA weights: large early while the EMA lags, then roughly flat. |  |  |
| `6 optim/‖θ − θ_start‖ / ‖θ_start‖` | Relative distance of the current weights from the starting checkpoint (<--ckpt>, after the head rescale): how far training has moved the model. Logged at probe steps. |  |  |
| `7 batch/null-label fraction` | Fraction of the batch whose label was replaced by the null class (CFG dropout, p = 0.1). | ≈ p | `train/cfg_drop_frac` |
| `7 batch/w(t) mean \| min \| max` | mean \| min \| max of the time weight w(t) since the last log (mean: average of batch means). |  | `train/time_weight_mean \| min \| max` |
| `7 batch/effective samples  (Σw)²/Σw²` | Effective sample size of a weighted micro-batch of B = 32; B / ESS is the variance cost of w(t). | ↑ | `train/time_weight_ess` |
| `7 batch/images seen` | Training images processed, all GPUs and micro-batches. |  |  |
| `7 batch/epochs` | images seen / 1,281,167 (ImageNet-1k train split). |  |  |
| `8 system/images per second` | Training throughput since the last log (or validation), all GPUs. | ↑ | `train/samples_per_s` |
| `8 system/seconds per step` | Wall seconds per optimizer step since the last log. | ↓ |  |
| `8 system/data wait fraction` | Fraction of wall time spent waiting for the next batch; high = input pipeline is the bottleneck. | ↓ |  |
| `8 system/peak GPU memory GB` | Peak allocated CUDA memory on rank 0 since the last log. |  |  |
| `(not logged)` | Duplicate of the wandb step axis. |  | `train/step` |

# Stage 2: flow-map distillation (`scripts/train_stage2_flowmap.py`)

Registered under the keys the Stage-2 trainer logs today; the readable name is what the key becomes once the trainer logs through `ReadableLogger`.

### Notation

| symbol | meaning |
|---|---|
| x1, z, t, x_t | data latent (d = 4096 entries), noise, interpolation time, x_t = (1 − t)·z + t·x1 |
| T(x, c) | frozen Stage-1 field (the teacher), EMA weights, class c or the null class |
| b(x) | teacher target field. Guided mode: CFG w with norm matching, so ‖b‖ = ‖T(x, y)‖; branch mode: T(x, y) with the student's label |
| v(x, σ, y) | the student network |
| X_σ(x) | x + σ·v(x, σ, y): the flow map, a jump of size σ along dx/dτ = b(x) |
| σ | jump size in [0, 1]; the jump covers ODE time τ = −log(1 − σ); X_1 jumps to the equilibrium |
| a ⊕ b | a + b − ab: composing jumps a then b gives a jump of a ⊕ b |
| ‖r‖² | squared L2 norm summed over all d entries (divide by 4096 for a per-entry value) |
| AW(r²) | adaptive weighting mean_i r_i² / (sg(r_i²) + ε)^p; with p = 0.5 it is ≈ mean ‖r‖ (unsquared) |
| raw | unweighted batch mean of ‖r‖², no adaptive weight |

### Metrics

Sections are numbered so the workspace lists them in reading order. Good: ↓ lower is better, ↑ higher, → target value, blank = context only.

| readable key | definition | good | key logged today |
|---|---|---|---|
| `1 loss/total (optimized)` | w_diag·diag + w_distill·distill + w_term·terminal + w_tnorm·terminal-at-teacher-eq + w_snorm·terminal-at-student-eq + w_inv·invariance (+ μ·time head). | ↓ | `train/loss` |
| `1 loss/diagonal  AW‖v(x_t, 0) − b(x_t)‖²` | At σ = 0 the student's velocity must equal the teacher field (the map's derivative at the start). | ↓ | `train/diag_loss` |
| `1 loss/distill  (lag_semi: lag + λ_semi·semi)` | Off-diagonal distillation term of the chosen --objective, averaged over micro-batches. | ↓ | `train/distill_loss` |
| `1 loss/Lagrangian  AW‖(1−σ)∂σX_σ − b(X_σ)‖²` | The map moves along the teacher field: its σ-derivative (forward JVP) matches b at the map's output. Last micro-batch only, so it does not add up to distill. | ↓ | `train/lag_loss` |
| `1 loss/semigroup  AW‖X_σ − X_σ⁻(X_σ')‖²` | One long jump equals two shorter jumps (σ' ⊕ σ⁻ = σ); the two-jump target is stop-gradient. Last micro-batch only. | ↓ | `train/semi_loss` |
| `1 loss/terminal keeps data  ‖X_1(x1) − x1‖²` | The full jump must leave clean data unchanged (data are equilibria). Unweighted, squared. | → 0 | `train/terminal_loss` |
| `1 loss/terminal at teacher eq.  AW‖b(X_1(z))‖²` | The one-step output from noise must be an equilibrium of the teacher field (gradient flows through the frozen teacher). Small random subset of the batch. | → 0 | `train/terminal_teacher_norm_loss` |
| `1 loss/terminal at student eq.  AW‖v(X_1(z), 0)‖²` | Same, with the student's own σ = 0 velocity. 0 when its weight is 0. | → 0 | `train/terminal_student_norm_loss` |
| `1 loss/terminal invariance  AW‖X_1(x_t) − X_1(x_t + δb)‖²` | Moving the start point along the teacher field must not change the endpoint. 0 when its weight is 0. | → 0 | `train/terminal_invariance_loss` |
| `1 loss/diagonal raw  ‖v(x_t, 0) − b‖²` | Unweighted mean of the diagonal residual. | ↓ | `train/diag_raw` |
| `1 loss/distill raw  ‖r‖²` | Unweighted mean of the objective's residual (lag_semi: the Lagrangian part). | ↓ | `train/distill_raw` |
| `1 loss/distill worst sample  max ‖r‖²` | Largest per-sample distill residual (max per micro-batch, averaged). | ↓ | `train/distill_raw_max` |
| `1 loss/semigroup raw  ‖X_σ − X_σ⁻(X_σ')‖²` | Unweighted mean of the semigroup residual. | ↓ | `train/semi_raw` |
| `1 loss/time head  E(t̂ − t)²` | Auxiliary noise-level head (weight μ). | ↓ | `train/time_pred_loss` |
| `1 loss/time head  E\|t̂ − t\|` | Mean absolute error of the noise-level head. | ↓ | `train/time_pred_mae` |
| `2 residual by t/diagonal ‖v(x_t, 0) − b‖²  t∈[lo, hi)` | Diagonal residual over samples whose base point has t in the bucket. Empty buckets count as 0 in the micro-batch mean (biased low at small batch). Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | ↓ | `train/diag_raw_t{lo}_{hi}` |
| `2 residual by t/distill ‖r‖²  t∈[lo, hi)` | Distill residual over samples whose base point has t in the bucket (empty buckets count as 0). Buckets: t∈[0, 0.1), t∈[0.1, 0.5), t∈[0.5, 0.9), t∈[0.9, 1]. | ↓ | `train/distill_raw_t{lo}_{hi}` |
| `3 residual by σ/distill ‖r‖²  σ∈[lo, hi)` | Distill residual over samples whose jump size σ is in the bucket (lag_semi: the Lagrangian σ). A bucket above --sigma-max always reads 0. Buckets: σ∈[0, 0.5), σ∈[0.5, 0.9), σ∈[0.9, 0.99), σ∈[0.99, 1]. | ↓ | `train/distill_raw_s{lo}_{hi}` |
| `3 residual by σ/semigroup ‖X_σ − X_σ⁻(X_σ')‖²  σ∈[lo, hi)` | Semigroup residual over samples whose outer jump σ is in the bucket. Buckets: σ∈[0, 0.5), σ∈[0.5, 0.9), σ∈[0.9, 0.99), σ∈[0.99, 1]. | ↓ | `train/semi_raw_s{lo}_{hi}` |
| `4 map vs teacher ODE/‖X_σ(z) − ODE_σ(z)‖  σ={0.5, 0.9, 0.97, 0.999}` | Norm (not squared, over 4096 entries) of one student jump from noise minus 100 Euler steps of the teacher ODE over the same τ; 16 fixed samples, EMA student. Has a floor from the reference's Euler error. | ↓ | `val/map_err_s{s}` |
| `4 map vs teacher ODE/‖b(X_1(z))‖ one-step endpoint` | Teacher field at the one-step sample from noise: 0 = the map lands on a teacher equilibrium. 16 fixed samples, EMA student. | → 0 | `val/endpoint_b_norm` |
| `4 map vs teacher ODE/‖b(X_1(z))‖ train batch` | Same quantity on the training subset used by the terminal-at-teacher-equilibrium loss (unsquared). | → 0 | `train/terminal_teacher_endpoint_norm` |
| `4 map vs teacher ODE/‖v(X_1(z), 0)‖ train batch` | Student σ = 0 velocity at the one-step endpoint (0 when that loss is off). | → 0 | `train/terminal_student_endpoint_norm` |
| `4 map vs teacher ODE/‖X_1(x_t) − X_1(x_t + δb)‖` | Endpoint change when the start point moves along b (0 when that loss is off). | → 0 | `train/terminal_invariance_endpoint_delta` |
| `5 samples/1 jump  X_1(z)` | One network evaluation from noise, EMA student. |  | `val/terminal_samples` |
| `5 samples/K jumps  X_s applied K×` | K equal jumps of size s = 1 − ε_stop^(1/K), composing to σ = 0.999; EMA student. |  | `val/offdiag_K{K}_samples` |
| `5 samples/Euler on v(x, 0), 100 steps` | Student used as a field (σ = 0) integrated like Stage 1; no CFG at sampling (guidance is baked in). |  | `val/diag_euler_samples` |
| `5 samples/Euler ‖v‖ at first step` | Mean ‖v(z, 0)‖ at the start. |  | `val/diag_velocity_norm_first` |
| `5 samples/Euler ‖v‖ at last step` | Mean ‖v(x, 0)‖ where the Euler sampler stops; → 0 = ended at a fixed point. | → 0 | `val/diag_velocity_norm_last` |
| `5 samples/Euler ‖x‖ at last step` | Should sit at the data-latent norm. |  | `val/diag_latent_norm_last` |
| `6 optim/learning rate` | Warmup, constant, optional cosine decay; lr_override.json can change it. |  | `train/lr` |
| `6 optim/‖grad‖ before clip` | Global gradient norm before clipping. |  | `train/grad_norm` |
| `6 optim/skipped steps (since restart)` | Steps skipped because the gradient norm was non-finite or above --grad-skip-norm. Resets on restart. | → 0 | `train/grad_skips` |
| `7 batch/null-label fraction` | Fraction of labels replaced by the null class. |  | `train/cfg_drop_frac` |
| `7 batch/mean jump size σ` | Batch mean of the sampled σ (sanity check of the σ law). |  | `train/sigma_mean` |
| `7 batch/mean base time t` | Batch mean of the base-point t (uniform: 0.5). |  | `train/interp_t_mean` |
| `7 batch/global batch` | batch × GPUs × accumulation (constant). |  | `train/effective_global_batch` |
| `7 batch/accumulation steps` | Micro-batches per optimizer step (constant). |  | `train/grad_accum_steps` |
| `8 system/images per second` | Global images per second since the last log (includes validation time). | ↑ | `train/samples_per_s` |
| `9 meanflow/…` | MeanFlow objective diagnostics (only for --objective meanflow/scaled_meanflow). |  | `train/meanflow_{what}` |

### Caveats

- `1 loss/Lagrangian` and `1 loss/semigroup` come from the last micro-batch, `1 loss/distill` from all micro-batches, so they do not add up.
- Bucket means count empty buckets as 0 inside each micro-batch, so small buckets read low at micro-batch 8.
- σ buckets above --sigma-max (e.g. σ∈[0.99, 1] at σ_max = 0.97) are always 0.
- Only rank 0's micro-batches are logged; nothing is all-reduced.

